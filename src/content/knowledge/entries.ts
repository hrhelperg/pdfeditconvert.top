import type { KnowledgeContent } from "@/types/content";

// Knowledge entries are registered here (one import per entry, alphabetical by
// slug) and collected into ENTRIES. This barrel is assembled from the authored
// content files during each production wave.

export const ENTRIES: KnowledgeContent[] = [
  // (populated by Waves 1–10)
];
